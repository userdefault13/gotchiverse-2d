/**
 * Stub for Next/Turbopack resolution only.
 * The real physicsManager lives on the legacy Gotchiverse API server.
 * shared_code treats this as unavailable unless getAllBoundsByLabel exists.
 */
module.exports = {};
